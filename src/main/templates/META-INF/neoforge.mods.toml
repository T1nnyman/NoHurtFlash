modLoader = "javafml"
loaderVersion = "${loader_version_range}"
license = "MIT"

[[mods]]
modId = "${mod_id}"
version = "${mod_version}"
displayName = "${mod_name}"
authors = "${mod_authors}"
description = '''${mod_description}'''
side = "CLIENT"

[[mixins]]
config = "${mod_id}.mixins.json"

[[mixins]]
config = "${mod_id}_geckolib.mixins.json"
requiredMods = ["geckolib"]

[[mixins]]
config = "${mod_id}_cobblemon.mixins.json"
requiredMods = ["cobblemon"]

[[dependencies."${mod_id}"]]
modId = "minecraft"
type = "required"
versionRange = "${minecraft_version_range}"
ordering = "NONE"
side = "CLIENT"

[[dependencies."${mod_id}"]]
modId = "geckolib"
type = "optional"
versionRange = "[4.9,)"
ordering = "AFTER"
side = "CLIENT"

[[dependencies."${mod_id}"]]
modId = "cobblemon"
type = "optional"
versionRange = "[1.7.3,1.7.4)"
ordering = "AFTER"
side = "CLIENT"
